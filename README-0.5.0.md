# Radar Editorial 0.5.0 — Administração e acesso

Configure na Hostinger `ADMIN_EMAIL` e `ADMIN_PASSWORD` antes do primeiro acesso. O primeiro administrador é criado automaticamente. As tabelas `users` e `sessions` são criadas automaticamente e as investigações antigas são atribuídas ao primeiro administrador.
