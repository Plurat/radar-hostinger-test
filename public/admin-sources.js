(async()=>{const d=await RadarUI.auth();if(!d||d.user.role!=='admin'){location.href='/';return}const area=document.getElementById('recommended-sources-area'),suggest=document.getElementById('source-suggestions-area');async function load(){const r=await fetch('/api/admin/recommended-sources'),rows=await r.json();area.innerHTML=rows.map(x=>`<div class="admin-source-row"><div><strong>${RadarUI.esc(x.name)}</strong><span>${RadarUI.esc(x.domain)} · ${RadarUI.esc(x.category)} · prioridade ${x.priority}</span></div><div class="form-actions"><button class="secondary" data-toggle="${x.id}">${x.active?'Desativar':'Ativar'}</button><button class="secondary" data-delete="${x.id}">Excluir</button></div></div>`).join('')||'<div class="empty">Nenhuma fonte.</div>';area.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=async()=>{const x=rows.find(x=>String(x.id)===b.dataset.toggle);await fetch('/api/admin/recommended-sources/'+b.dataset.toggle,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({active:!x.active})});load()});area.querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{if(confirm('Excluir esta fonte?')){await fetch('/api/admin/recommended-sources/'+b.dataset.delete,{method:'DELETE'});load()}})}async function loadSuggestions(){const r=await fetch('/api/admin/source-suggestions'),rows=await r.json();suggest.innerHTML=rows.length?rows.map(x=>`<div class="admin-source-row"><div><strong>${RadarUI.esc(x.domain)}</strong><span>${RadarUI.esc(x.example_title||'Sem título')} · ${RadarUI.esc(x.user_name||x.user_email||'Usuário')}</span><a href="${RadarUI.esc(x.example_url||'#')}" target="_blank" rel="noopener">Ver exemplo</a></div><div class="form-actions"><button data-approve="${x.id}">Adicionar</button><button class="secondary" data-reject="${x.id}">Ignorar</button></div></div>`).join(''):'<div class="empty">Nenhuma sugestão pendente.</div>';suggest.querySelectorAll('[data-approve]').forEach(b=>b.onclick=async()=>{const row=rows.find(x=>String(x.id)===b.dataset.approve);const name=prompt('Nome da fonte:',row.domain);if(!name)return;await fetch('/api/admin/source-suggestions/'+b.dataset.approve+'/approve',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,category:'web',priority:50})});load();loadSuggestions()});suggest.querySelectorAll('[data-reject]').forEach(b=>b.onclick=async()=>{await fetch('/api/admin/source-suggestions/'+b.dataset.reject+'/reject',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});loadSuggestions()})}document.getElementById('source-form').onsubmit=async e=>{e.preventDefault();const body={name:document.getElementById('rs-name').value,domain:document.getElementById('rs-domain').value,category:document.getElementById('rs-category').value,priority:+document.getElementById('rs-priority').value,description:document.getElementById('rs-description').value};const r=await fetch('/api/admin/recommended-sources',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok){const x=await r.json();alert(x.error);return}e.target.reset();load()};document.getElementById('refresh-suggestions').onclick=loadSuggestions;load();loadSuggestions()})();
